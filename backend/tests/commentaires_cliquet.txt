# Baseline du plafond de commentaires (regle 13, ADR-0099) — un chemin, une espace, un compte.
#
# ETAT : VIDE depuis E00US027. Le perimetre du garde-fou est **tout le code de production**
# backend : les cinq couches, plus `atlas/`, `release/` et les points d'entree racine — soit
# 1048 blocs resorbes. Restent **hors porte** `backend/tests/` et `backend/migrations/`, ce qui
# est trace en `DETTE-088` ; le front, lui, couvre ses propres tests.
#
# Le fichier n'est plus une baseline mais une soupape, et `test_le_cliquet_est_vide` la garde :
# y ajouter une ligne oblige a supprimer ce test, donc produit un diff que la revue voit.
