# Baseline du plafond de commentaires (ADR-0099, regle 13) — genere par E00US027.
#
# Chaque ligne : <chemin relatif a backend/> <nombre de blocs > 8 lignes toleres>.
# CLIQUET : un chiffre ne se releve JAMAIS. Toute US qui touche un fichier fait baisser le
# sien ; un fichier absent d'ici n'a droit a aucun bloc long. Quand un chiffre atteint 0,
# retirer sa ligne.
#
# ETAT : VIDE depuis E00US027 — le backend est integralement sous le plafond (1048 blocs
# resorbes). La regle est donc DURE : tout bloc de plus de huit lignes fait echouer la suite,
# ou qu'il soit. Ce fichier n'est plus une baseline mais une soupape : y ajouter une ligne est
# une derogation a justifier en revue, jamais un geste de routine.
